# おへやあそび: モバイル確認ポイント

今回の修正では、既存の横長Before / After画像を画面内でクロップせず表示し、2人の子どもが同時に見えることを優先する。

確認項目:

- 右側の子が画面端で切れない。
- ドラッグ対象の積み木は中央に浮かず、左側の遊び領域の中に自然に見える。
- ドラッグ開始時だけ少し浮いた反応が出る。
- 右側の子の近くへドロップすると `bring-toy` が成立してAfterへ切り替わる。
- 左側の遊び領域へ戻すと `keep-playing` が成立する。
- 正解領域や操作方法を可視の文字・枠・矢印では表示しない。
