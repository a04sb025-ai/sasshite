# 開発方針

このリポジトリは、初心者がAIと相談しながら小さく継続開発できることを最優先にします。

## 基本方針

- React、Vite、TypeScriptによる静的なSPAを維持する。
- 依存パッケージは必要最小限にし、単純な機能は標準のReact・CSSで実装する。
- ゲーム進行、問題データ、採点、見た目の責務を分ける。場面ごとの巨大な画面コンポーネントを複製しない。
- 問題の文言、採点値、制限時間は `src/data/` に集約する。
- スコア計算は副作用のない関数にし、テストしやすくする。
- 場面の背景や人物には、オリジナルのローカル画像アセットを利用してよい。操作対象と判定はHTML/SVGレイヤーに分離し、画像だけにゲーム進行を依存させない。
- 生成画像を利用する場合も既存作品の固有表現を模倣せず、プロンプトと生成手順をリポジトリ内で再確認できるようにする。APIキーをフロントエンドやコミットへ含めない。

## UIとアクセシビリティ

- 320px程度の縦長画面から使えるモバイルファーストのレイアウトにする。
- タップ対象は原則44px以上を確保し、キーボード操作と見えるフォーカス表示にも対応する。
- 色だけで状態を伝えず、必要に応じてテキストや形の変化も併用する。
- 動きは操作の反応を示す程度に抑え、`prefers-reduced-motion` を尊重する。
- 説明を増やしすぎず、情景から操作を察する体験を守る。

## 変更時の確認

- `npm run typecheck`
- `npm test`
- `npm run build`
- UIを変更した場合は、可能ならスマートフォン相当の幅でも目視確認する。

新機能は一度に大きく追加せず、問題データ、ロジック、UI、テストの順に追える小さな変更にしてください。
