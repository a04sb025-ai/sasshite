# 察して。

> あなたなら、どうする？

説明されていない小さな気まずさや迷いを眺め、自分ならどう動くかを選ぶスマートフォン向けの短編Webゲームです。

Ver.0.5では「正解っぽい場所を当ててタップするゲーム」から方針を変え、**状況を見る → 行動を選ぶ → 結果の小さな変化を見る**という流れに作り直しています。正解数は出さず、5つの場面での選び方から「察知力・気遣い・主張力・鋼メンタル」を診断します。

## 遊び方

1. タイトル画面の「はじめる」を押します。
2. 場面の絵と短い状況文を見ます。
3. 「あなたなら？」に対して3つの行動から選びます。
4. 選ばずに時間が過ぎた場合も、その場面でのひとつの回答として進みます。
5. 行動すると、その後に起きた小さな変化だけが表示されます。
6. 5場面が終わると、正解ではなく「あなたの察し方」が表示されます。

各場面に唯一の正解はありません。気遣いだけでなく、主張する・待つ・気にしないといった選択にもそれぞれ性格が出る設計です。

## Ver.0.5の5場面

- 電車: 隣席のバッグと、立っている人
- エレベーター: 閉まり始めた扉と、走ってくる人
- 最後のひとつ: 4人の食卓に残った唐揚げ
- オンライン会議: 意見を求められたあとの沈黙
- 帰り際: 終了後、出口近くに落ちた紙くず

## 開発

必要環境: Node.js 20以降

```bash
npm install
npm run dev
```

| コマンド | 内容 |
| --- | --- |
| `npm run dev` | 開発サーバーを起動 |
| `npm run build` | TypeScriptの検査後、配信用ファイルを生成 |
| `npm run deploy` | `dist/` をCloudflare Workers Static Assetsへ公開 |
| `npm run typecheck` | TypeScriptのみ検査 |
| `npm test` | ゲーム進行と判定のテスト |

## 公開

`npm run build` で生成される `dist/` を、Cloudflare Workers Static Assetsとして公開します。設定は `wrangler.jsonc` にあり、SPA用のフォールバックも有効にしています。Workerのサーバーサイドスクリプトは使用しません。

ローカルから初回デプロイする場合は、Cloudflareへログインしてからビルド・デプロイします。

```bash
npx wrangler login
npm run build
npm run deploy
```

CloudflareのGit連携を使う場合は、ビルドコマンドを `npm run build`、デプロイコマンドを `npx wrangler deploy` に設定します。

## 構成

- `src/data/`: 状況文、選択肢、反応文、採点値、制限時間
- `src/game/`: スコア計算と診断ロジック
- `src/components/`: 場面・イラスト・共通UI
- `src/App.tsx`: タイトルから結果までの進行管理

場面を追加するときは、まず `src/data/scenes.ts` にデータを加え、必要な見た目だけを `SceneArtwork.tsx` に追加します。開発方針の詳細は [`AGENTS.md`](./AGENTS.md) を参照してください。

## 今後

まずはVer.0.5で「何をするゲームなのかが初見で分かること」と「選んだ結果に自分らしさが出ること」を実機で確認します。その後、場面数、演出、効果音、結果の共有などを追加していきます。
